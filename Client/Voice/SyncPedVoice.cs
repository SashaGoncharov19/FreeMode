namespace GTANetwork.Sync
{
    internal partial class SyncPed
    {
        /// <summary>Until when (Program clock, ms) this player counts as talking; the nametag shows an indicator (T-016).</summary>
        internal long TalkingUntilMs;
    }
}
